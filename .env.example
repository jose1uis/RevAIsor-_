# Reference only: the program reads environment variables, not this file.
# Set these in your shell; never commit a real API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional independent bonus evaluator model.
OPENAI_EVALUATOR_MODEL=gpt-4.1-mini
